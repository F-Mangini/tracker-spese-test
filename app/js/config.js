/* ============================================
   CONFIG
   ============================================ */

window.SPESA_TRACKER_CONFIG = window.SPESA_TRACKER_CONFIG || {
    storageKey: 'spesa-tracker-data',
    channel: 'stable'
};
